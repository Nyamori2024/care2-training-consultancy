// src/contexts/FormContext.jsx

// Importing createContext to create a new context
import { createContext } from "react";

// Creating a context for form data and handlers
export const FormContext = createContext();
